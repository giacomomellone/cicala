# input

EC11 rotary encoder on the PCNT peripheral (quadrature decode) plus the push button with debounce and short/long-press (1.5 s) detection. The complete interaction surface is turn, press, long-press, and idle. Nothing else exists. Lands in phase C.
