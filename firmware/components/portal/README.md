# portal

SoftAP + DNS-hijack captive portal for first-time Wi-Fi credentials **and language selection** (which bundles to install, max two). Entered only from the OLED menu; the e-paper is never touched. Lands in phase C.
