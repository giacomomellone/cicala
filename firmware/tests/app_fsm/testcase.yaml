tests:
  tischkarte.app_fsm:
    # Time is injected and there is no display, so this needs no board features
    # beyond a kernel.
    platform_allow:
      - qemu_xtensa/dc233c
      - native_sim
      - native_sim/native/64
    integration_platforms:
      - qemu_xtensa/dc233c
    tags:
      - tischkarte
      - app_fsm
