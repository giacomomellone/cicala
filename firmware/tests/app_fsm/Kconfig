# The refresh timeout is a CONFIG_TK_* value and the suite asserts on both
# sides of it, so it sources the same file the application builds with.

mainmenu "tischkarte app_fsm tests"

rsource "../../Kconfig.policy"

source "Kconfig.zephyr"
