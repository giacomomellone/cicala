# The application's policy symbols: the block contains the shuffle bag, whose
# size follows CONFIG_TK_MAX_QUESTIONS and CONFIG_TK_RECENT_RING. A suite that
# built against different values would be checking a different struct.

mainmenu "tischkarte retained tests"

rsource "../../Kconfig.policy"

source "Kconfig.zephyr"
