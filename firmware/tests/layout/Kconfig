# The application's policy symbols: the panel geometry and the question buffer
# size are CONFIG_TK_* values, and the point of this suite is that the shipped
# corpus fits the values the device actually builds with.

mainmenu "tischkarte layout tests"

rsource "../../Kconfig.policy"

source "Kconfig.zephyr"
