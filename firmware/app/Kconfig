# Application Kconfig. The policy symbols live one level up in
# ../Kconfig.policy so the test suites build against the same values rather
# than a copy that drifts.

mainmenu "tischkarte"

rsource "../Kconfig.policy"

source "Kconfig.zephyr"
