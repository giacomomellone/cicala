# APP_DIR gives the application and MCUboot one absolute signing-key path.
# Define this before the upstream symbol because its first default wins.

config BOOT_SIGNATURE_KEY_FILE
	default "$(APP_DIR)/../keys/firmware-signing-dev.pem" if BOOT_SIGNATURE_TYPE_ED25519

source "share/sysbuild/Kconfig"
