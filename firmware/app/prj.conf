# Base application configuration, shared by every board.

CONFIG_CPP=y
CONFIG_STD_CPP17=y
CONFIG_CPP_EXCEPTIONS=n
CONFIG_CPP_RTTI=n
CONFIG_REQUIRES_FULL_LIBCPP=n

CONFIG_LOG=y
CONFIG_LOG_MODE_DEFERRED=y
CONFIG_LOG_BUFFER_SIZE=2048

# Deferred logging measured 912 bytes against the 1024-byte default.
CONFIG_LOG_PROCESS_THREAD_STACK_SIZE=2048

CONFIG_GPIO=y
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=1536

# The bag picks a question with sys_rand32_get().
CONFIG_ENTROPY_GENERATOR=y

# cfb_framebuffer_init() calls k_malloc, and Zephyr's default heap is zero.
CONFIG_HEAP_MEM_POOL_SIZE=8192

# Fail loudly in development rather than continuing with a bad state.
CONFIG_ASSERT=y

# Why the chip started.
CONFIG_HWINFO=y

CONFIG_ZBUS=y
CONFIG_ZBUS_CHANNEL_NAME=y
CONFIG_ZBUS_MSG_SUBSCRIBER=y
CONFIG_ZBUS_MSG_SUBSCRIBER_BUF_ALLOC_STATIC=y
CONFIG_ZBUS_RUNTIME_OBSERVERS=n

# Size the static subscriber buffer for the largest channel message.
CONFIG_ZBUS_MSG_SUBSCRIBER_NET_BUF_STATIC_DATA_SIZE=144

CONFIG_INPUT=y

# Run callbacks on the driver workqueue without a dedicated input thread.
CONFIG_INPUT_MODE_SYNCHRONOUS=y

CONFIG_SPI=y
CONFIG_MIPI_DBI=y
CONFIG_DISPLAY=y
CONFIG_CHARACTER_FRAMEBUFFER=y
