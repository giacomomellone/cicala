# Applied by the soak and retain firmware profiles.

CONFIG_CICALA_DEBUG_SOAK=y

# Suppress full refreshes so ghosting accumulates.
CONFIG_CICALA_FULL_REFRESH_INTERVAL=65535

CONFIG_CICALA_NET=n
CONFIG_WIFI=n
CONFIG_NETWORKING=n
