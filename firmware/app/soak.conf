# Applied by the soak and retain firmware profiles.

CONFIG_KVELD_DEBUG_SOAK=y

# Suppress full refreshes so ghosting accumulates.
CONFIG_KVELD_FULL_REFRESH_INTERVAL=65535

CONFIG_KVELD_NET=n
CONFIG_WIFI=n
CONFIG_NETWORKING=n
