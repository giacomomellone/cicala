# Applied by the soak and retain firmware profiles.

CONFIG_TK_DEBUG_SOAK=y

# Suppress full refreshes so ghosting accumulates.
CONFIG_TK_FULL_REFRESH_INTERVAL=65535

CONFIG_TK_NET=n
CONFIG_WIFI=n
CONFIG_NETWORKING=n
