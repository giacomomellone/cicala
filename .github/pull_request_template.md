<!-- Delete any section that does not apply. -->

## What this changes

## Why

## Checklist

- [ ] `just validate` passes.
- [ ] `just test` passes.
- [ ] Docs updated if behaviour or architecture changed; new `docs/` pages have a `nav:` entry.
- [ ] Durable design decisions and rejected alternatives recorded in `docs/decisions.md`.
- [ ] **I read the full diff and it contains no credentials, supplier quotations, private commercial strategy, or participant personal data.** This repository is public. `.gitignore` is not the boundary — it only affects `git add`, and it does nothing about material pasted into a document, a test fixture, a comment, or a commit message. See [docs/publication_policy.md](../docs/publication_policy.md).
