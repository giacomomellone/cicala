# Python dependencies for tools/ and docs. Install into a venv:
#   python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt
# (or simply: just setup)
#
# Dependency policy (docs/decisions.md): tools use stdlib + these two only.
pyyaml>=6.0
jsonschema>=4.21

# docs site (mkdocs.yml) — not a tools dependency
mkdocs-material>=9.5

# dev-only: formatter + linter for tools/ (just fmt-py / just lint-py).
# Not imported by any tool, so the stdlib+2 runtime policy is intact.
ruff>=0.6
